indexOf方法返回给定元素在数组第一次出现的位置，如果没有返回-1
可以有第二个参数，表示从第二个参数所指的位置开始搜索

var a = ['a', 'b', 'c'];

a.indexOf('b') // 1
a.indexOf('y') // -1

lastIndexOf方法返回给定元素在数组中最后一次出现的位置，如果没有就返回-1


如果是NaN则会返回-1，因为NaNcy不等于本身