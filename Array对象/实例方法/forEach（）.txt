对数组的所有成员依次执行参数函数，forEach和map类似，只是forEach没有返回值
如果数组遍历的目的是为了得到返回值，那么使用map，否则用forEach
参数是一个函数，该函数接受三个参数，当前值（elem）、当前位置（index)、整个数组

forEach一样可以有第二个参数，绑定参数函数的this变量

这个方法无法中断执行，会把所有成员都遍历完


会跳过空位，不会跳过undefined和null