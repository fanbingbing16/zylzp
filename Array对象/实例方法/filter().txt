过滤数组成员，参数是一个参数，所有成员都会依次执行该函数，满足条件的成员组成一个新数组返回，不会改变原数组
[1, 2, 3, 4, 5].filter(function (elem) {
  return (elem > 3);
})
// [4, 5]

filter()方法的参数函数可以接受三个参数：当前成员，当前位置和整个数组。

filter()方法还可以接受第二个参数，用来绑定参数函数内部的this变量。

